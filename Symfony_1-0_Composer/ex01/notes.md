# in this exercise, many versions of monolog have to be install in different substack

I scripted a bash file to do that:

- 5 subfiles with each a specific composer.json file to configure versions of Monolog
- then  apply composer install in each task directory
